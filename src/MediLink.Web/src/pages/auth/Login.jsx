import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
import axios from 'axios'
const apiUrl = import.meta.env.VITE_API_URL || 'http://localhost:5140/api'
export default function Login(){const [form,setForm]=useState({email:'',password:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false),nav=useNavigate(); const submit=async e=>{e.preventDefault();setLoading(true);setError('');try{const {data}=await axios.post(`${apiUrl}/auth/login`,form);localStorage.setItem('medilink-token',data.token);localStorage.setItem('medilink-user',JSON.stringify(data));nav('/')}catch{setError('Unable to sign in. Check your email and password.')}finally{setLoading(false)}};return <section className="auth-page"><form className="auth-card" onSubmit={submit}><Link className="brand" to="/"><span>✚</span> MediLink</Link><h1>Welcome back</h1><p>Sign in to manage your healthcare orders.</p>{error&&<div className="form-error">{error}</div>}<label>Email address<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="you@example.com"/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Your password"/></label><button className="primary" disabled={loading}>{loading?'Signing in…':'Sign in'}</button><p className="switch">New to MediLink? <Link to="/register">Create an account</Link></p></form></section>}
